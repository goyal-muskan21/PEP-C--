#include<bits/stdc++.h>
using namespace std;

class trees {
     public:
     int data;
     trees *left;
     trees *right;
     int value;
     void min_heap(value) {
          if (left != NULL) {
               min_heap(left);
     }
};

